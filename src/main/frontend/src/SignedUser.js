import React, {useState, useEffect} from 'react';
import axios from 'axios';

function SignedUser() {
    return (
        <div>
            Hello, SignedUser!!!
        </div>
    )
}

export default SignedUser;